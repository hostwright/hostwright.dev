---
---

<div class="term-live" aria-label="Confirmed local lifecycle commands">
  <div class="tl-bar">
    <span class="tl-dots" aria-hidden="true"><i></i><i></i><i></i></span>
    <span class="tl-title">hostwright lifecycle</span>
    <span class="tl-tag">confirm each change</span>
  </div>

  <pre class="tl-body"><code>{
    [
      { c: "muted", t: "# Signed, authenticated local daemon required" },
      { c: "cmd", t: "$ hostwright validate hostwright.yaml" },
      { c: "cmd", t: "$ hostwright up hostwright.yaml --dry-run" },
      { c: "muted", t: "# Review images, resources, ownership and actions" },
      { c: "muted", t: "# Copy the reviewed planSHA256" },
      { c: "cmd", t: "$ hostwright up hostwright.yaml \\" },
      { c: "cmd", t: "    --confirm-plan <planSHA256>" },
      { c: "cmd", t: "$ hostwright status hostwright.yaml" },
      { c: "muted", t: "# Every later change needs a fresh reviewed hash" },
    ].map((l, i) => (
      <span class:list={["ln", l.c]} style={`--i:${i}`}>{l.t}<span class="cur" aria-hidden="true"></span></span>
    ))
  }</code></pre>
</div>

<style>
  .term-live {
    border: 1px solid var(--color-border-strong);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--color-card);
    box-shadow: 0 18px 40px -28px rgba(32, 35, 39, 0.45);
    max-width: 46rem;
  }
  .tl-bar {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
  }
  .tl-dots {
    display: inline-flex;
    gap: 6px;
  }
  .tl-dots i {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: var(--color-border-strong);
  }
  .tl-title {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }
  .tl-tag {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.04em;
    color: var(--color-text-faint);
  }

  .tl-body {
    margin: 0;
    padding: var(--space-4) var(--space-5);
    border: 0;
    border-radius: 0;
    background: transparent;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    line-height: 1.85;
    overflow-x: auto;
  }
  .tl-body code {
    display: block;
    background: none;
    border: 0;
    padding: 0;
    white-space: pre;
  }

  .ln {
    display: block;
    position: relative;
  }
  .ln.cmd { color: var(--color-accent); font-weight: var(--weight-medium); }
  .ln.muted { color: var(--color-text-faint); }
  .ln.head { color: var(--color-text); font-weight: var(--weight-semibold); }
  .ln.add { color: var(--status-implemented); }
  .ln.chg { color: var(--color-accent); }
  .ln.ok { color: var(--status-implemented); font-weight: var(--weight-medium); }
  .ln.blank { color: transparent; }

  /* Per-line cursor — hidden by default; only the active line shows a block
     caret while the transcript types out. */
  .cur {
    display: none;
    width: 0.55em;
    height: 1.05em;
    margin-left: 2px;
    vertical-align: -0.2em;
    background: var(--color-accent);
  }

  /* JS-enhanced state: hide lines, reveal on .play. */
  .term-live.anim .ln {
    opacity: 0;
    transform: translateY(4px);
  }
  .term-live.anim.play .ln {
    animation: ln-in 0.32s var(--ease) forwards;
    animation-delay: calc(var(--i) * 150ms);
  }
  /* A single resting caret blinks on the final line once the transcript has
     fully revealed. */
  .term-live.anim.play .ln.ok .cur {
    display: inline-block;
    opacity: 0;
    animation: blink 1.1s steps(1) calc(var(--i) * 150ms) infinite;
  }

  @keyframes ln-in {
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes blink {
    0%, 50% { opacity: 1; }
    50.01%, 100% { opacity: 0; }
  }

  @media (prefers-reduced-motion: reduce) {
    .term-live.anim .ln {
      opacity: 1;
      transform: none;
      animation: none;
    }
    .term-live.anim .ln .cur { display: none; }
    .term-live.anim .ln.ok .cur { display: inline-block; animation: blink 1.1s steps(1) infinite; }
  }
</style>

<script>
  const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  document.querySelectorAll<HTMLElement>(".term-live").forEach((el) => {
    el.classList.add("anim");
    if (reduce) {
      el.classList.add("play");
      return;
    }
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            el.classList.add("play");
            io.disconnect();
          }
        });
      },
      { threshold: 0.4 }
    );
    io.observe(el);
  });
</script>
