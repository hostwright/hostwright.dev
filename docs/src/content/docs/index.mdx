---
title: What Hostwright is
description: Hostwright is a Mac-native desired-state control plane for Apple container workloads. Start here.
---

import { Aside } from "@astrojs/starlight/components";

Hostwright is a **Mac-native desired-state control plane** for [Apple container](/concepts/apple-container-boundary/) workloads.

You declare local services in a restricted [`hostwright.yaml`](/reference/manifest/) manifest. Hostwright validates it, computes a deterministic non-mutating [plan](/concepts/reconciliation/), observes the runtime through a single typed [adapter boundary](/concepts/runtime-adapter/), persists state to an explicit SQLite path you choose, and mutates the runtime only through narrow, hash-confirmed gates.

It is built for one machine: an Apple silicon Mac running macOS 26 or newer with Apple container installed. It is not a cluster orchestrator, and it does not try to be.

<Aside type="caution" title="Source-only alpha">
Hostwright `v0.1.0-alpha.1` is a source-only alpha. It is not production ready.
There are no binary downloads, installers, Homebrew formulae, signed or notarized
artifacts. You [build it from source](/getting-started/install-from-source/).
Read [Limitations](/reference/limitations/) before relying on any behavior.
</Aside>

## What works today

- `hostwright init`, `validate`, and `plan` — restricted manifest parsing, fail-closed validation, and deterministic non-mutating planning with a plan hash.
- `hostwright status --state-db <path>` — live runtime observation through the RuntimeAdapter with event and snapshot persistence.
- `hostwright apply --state-db <path> --confirm-plan <hash>` — exactly **one** confirmed action per invocation: create a missing service, or a restart-policy-allowed managed start or managed restart.
- `hostwright logs`, `events`, `recovery`, `diagnostics` — bounded, redacted, local-only observability from the explicit state database.
- `hostwright cleanup` — dry-run classification first, then token-confirmed deletion of exact Hostwright-owned stopped/created/exited containers only.
- `hostwrightd --foreground` — a foreground reconciliation loop that observes, plans, runs loopback health probes, and records events. It never mutates the runtime.
- `keychain://` [secret references](/concepts/secrets-and-keychain/) in `secretEnv`, backed by an opt-in noninteractive read-only macOS Keychain reader.
- `hostwright doctor` — safe local checks and resource intelligence, with unmeasured dimensions reported as unmeasured.

Everything else — general lifecycle management, multi-action apply, unattended daemon mutation, DNS, tunnels, Kubernetes or Docker API compatibility, Compose parity — is a non-goal or not implemented. The [Limitations](/reference/limitations/) page is the authoritative list.

## How these docs are organized

| Section | Answers |
| --- | --- |
| **Getting started** | Build from source, run the safe core loop. |
| **Concepts** | How Hostwright thinks: desired state, the adapter, reconciliation, state, policy, safety, secrets. |
| **Tasks** | Step-by-step walkthroughs, each labeled read-only, state-writing, or mutating. |
| **Reference** | Exact surfaces: CLI, manifest, error codes, policy, doctor checks, limitations, compatibility. |
| **Operations** | Security and safety, team workflow, the daemon, events and diagnostics. |
| **Release** | The release process and release notes, source-only artifact policy included. |
| **Roadmap and research** | What is planned, deferred, rejected, or research-only — clearly labeled, never presented as current support. |

## Where to start

- New here? Read [Why Hostwright](/overview/why-hostwright/), then [Desired state](/concepts/desired-state/).
- Want the boundaries first? Read [Non-goals](/overview/non-goals/) and [Limitations](/reference/limitations/).
- Ready to try it? [Install from source](/getting-started/install-from-source/), then the [Quick start](/getting-started/quick-start/).
